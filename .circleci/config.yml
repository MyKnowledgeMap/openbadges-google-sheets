defaults: &defaults
  working_directory: ~/project
  docker:
    - image: circleci/node:8.9.4
     
version: 2
jobs:
  install-dependencies:
    <<: *defaults
    steps:
      - checkout
      - restore_cache:
          key: v1-deps-
      - run: npm install
      - save_cache:
          key: v1-deps-{{ .Revision }}
          paths:
          - node_modules
  test:
    <<: *defaults
    steps:
    environment:
      JEST_JUNIT_OUTPUT: reports/jest/jest-results.xml
    steps:
      - checkout
      - restore_cache:
          key: v1-deps-{{ .Revision }}
      - run: npm install
      - run: mkdir reports && mkdir reports/jest && mkdir reports/tslint
      - run: npm run lint:ci
      - run: npm run test:ci
      - store_test_results:
          path: reports
      - store_artifacts:
          path: reports
  build:
    <<: *defaults
    steps:
      - checkout
      - restore_cache:
          key: v1-deps-{{ .Revision }}
      - run: npm install
      - run: npm run build
      - store_artifacts:
          path: dist
          
workflows:
  version: 2
  build-and-test:
    jobs:
      - install-dependencies
      - test:
          requires:
            - install-dependencies
      - build:
          requires:
            - install-dependencies
