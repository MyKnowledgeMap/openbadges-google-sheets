# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.9.4
    working_directory: ~/openbadges-google-apps-script
    steps:
      # Checkout the source code.
      - checkout

      # Download and cache dependencies.
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package.json" }}
          - v1-dependencies-
      - run: npm install
      - save_cache:
          key: v1-dependencies-{{ checksum "package.json" }}
          paths:
            - node_modules
      
      # Build the app.
      - run: 
          name: "Build"
          command: npm run build
      
      # Test the app.
      - run: 
          name: "Test"
          environment:
            JEST_JUNIT_OUTPUT: reports/jest-results.xml
          command: mkdir reports && npm run lint -- --out reports/tslint-results.xml --format junit && npm run test:ci -- --ci --testResultsProcessor="jest-junit"

      # Store the test reports.
      - store_test_results:
          path: reports
      - store_artifacts:
            path: reports

      # Store the build artifacts.
      - store_artifacts:
          path: dist
